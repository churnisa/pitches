## Pitches-Private

Requires `personal access token` called `PITCHES_REPO_ACCESS_TOKEN`

Manually create the `public pitches` in the same organization. Workflow will push public contents of `private-pitches` to `pitches`.



